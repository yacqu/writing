{
  "name": "writing",
  "version": "2.0.0",
  "description": "A lightweight distraction-free markdown text editor with file system integration",
  "type": "module",
  "scripts": {
    "start": "tsx server/index.ts",
    "dev": "tsx watch server/index.ts",
    "tmux": "cli kp 3031 && tmux new-session -d -s writing 'npm run dev' && tmux split-window -h 'npx live-server --port=3031 --open=public' && tmux attach-session -t writing"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "live-server": "^1.2.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Joseph Ernest",
  "license": "MIT"
}
